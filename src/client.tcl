# - Lines from stdin are evaled in the slave interpreter.
# - SDL Input is encoded as a command and evaled in the slave interpreter.
# - The slave interpreter prints to stdandard output when it wants to
#   instigate a change.
