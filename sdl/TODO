* TODO Finish work with 'ents'
  The loop is in place; commands are almost done.

* TODO Implement 'Event Hooks'
  As it says in README

* TODO Cleanup Tcl proc definitions in Draw()
  There's lot of repitition.  Write some functions or macros to clean it
  up.  Also we don't support list arguments for e.g. circle yet.
